# Binaries
/hydra
*.exe

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test
/tmp/
redeploy-*.log

# Manual backups / working-tree junk
*.bak*
*.orig
*~
*.swp
*.broken*
*.prev
*.prev.*
typhon-engine.prev*

# build scratch (sendfile bench copies) — never track
.sendfile-build/

# Bytecode Python de l outillage de banc : genere, jamais lu par un humain.
tools/paritydiff/__pycache__/

# Cargo cache kept beside the worktree so a container build can reuse it.
# 330 MB of downloaded crates: a build artefact, not source.
/.cargo-home/

# Left behind by `cargo test`: the store opens a database beside the
# crate root when a test builds a manager without one of its own.
typhon-engine/hydra.db
